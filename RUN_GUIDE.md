# 🛡️ دليل تشغيل مشروع Cybersecurity AI Assistant

هذا الملف يحتوي على جميع الأوامر التي ستحتاجها لتشغيل المساعد الذكي، سواء عبر الواجهة الرسومية أو سطر الأوامر.

---

## 1. تفعيل البيئة الافتراضية (Virtual Environment)
يجب دائماً تفعيل البيئة قبل البدء لضمان عمل المكتبات:

```powershell
# تفعيل البيئة في PowerShell
.\.venv\Scripts\activate
```

---

## 2. تشغيل الموقع (Web Interface)
هذا هو الخيار الأفضل لاستخدام واجهة الدردشة الجميلة:

```powershell
# تأكد من أنك داخل المجلد الرئيسي للمشروع
python app.py
```
بعد التشغيل، افتح المتصفح واذهب إلى: `http://127.0.0.1:5000`

---

## 3. إدخال مستندات جديدة (Ingest Documents)
إذا أضفت ملفات PDF أو TXT جديدة في مجلد `data` وتريد من الذكاء الاصطناعي "قراءتها":

```powershell
python ingest.py
```

---

## 4. تشغيل المساعد في التيرمينال (CLI Bot)
إذا كنت تفضل الدردشة مباشرة داخل التيرمينال دون فتح المتصفح:

```powershell
python rag_bot.py
```

---

## 5. اختبار النموذج فقط (Test Model)
للتأكد من أن نموذج Llama-2 يعمل بشكل صحيح داخل Ollama:

```powershell
python test_model.py
```

---

## 💡 ملاحظات هامة:
1. **تأكد من تشغيل تطبيق Ollama** في الخلفية دائماً، لأن المشروع يعتمد عليه لتشغيل النموذج (`cybersec-assistant`) ونموذج الـ Embeddings.
2. **عند إضافة ملفات جديدة**: يجب تشغيل `ingest.py` ليقوم النظام بتحديث قاعدة البيانات (ChromaDB).
3. **لإغلاق أي عملية**: اضغط على `Ctrl + C` في التيرمينال.

---
© 2026 Cybersecurity AI Assistant Project
